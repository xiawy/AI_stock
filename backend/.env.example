# ── 本文件已并入项目根目录 .env（v0.5.14）────────────────────────────
# backend/ 现在默认从根目录 .env 读取全部服务变量（LLM key 与 SECRET_KEY
# 等共用同一份配置，单一事实来源）。
#
# 如需本地覆盖（例如单独调试 backend），把要覆盖的变量复制到本文件——
# backend/.env 中的值优先级高于根 .env。
#
# 可用变量见根目录 .env.example 的「Backend 服务」一节：
#   SECRET_KEY / ALGORITHM / ACCESS_TOKEN_EXPIRE_MINUTES /
#   DATABASE_URL / CORS_ORIGINS / PROJECT_ROOT / DEBUG
